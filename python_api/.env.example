# Example environment file for the Python API (copy to python_api/.env)
# Put secrets locally, never commit python_api/.env

# If your Python API needs Supabase (optional)
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# Example app settings
APP_ENV=development
APP_DEBUG=true
